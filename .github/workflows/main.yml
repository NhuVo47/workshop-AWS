# Disabled - using hugo.yml instead
name: Deploy Hugo site (disabled)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Using hugo.yml workflow instead"
